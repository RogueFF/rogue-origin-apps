{
  "name": "rogue-origin-api",
  "version": "1.0.0",
  "description": "Rogue Origin API - Cloudflare Workers",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml --env=\"\"",
    "deploy": "wrangler deploy --config wrangler.toml --env=\"\"",
    "tail": "wrangler tail",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "keywords": [
    "cloudflare",
    "workers",
    "api"
  ],
  "author": "Rogue Origin",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.59.2"
  }
}
