{
  "name": "mission-control-api",
  "version": "0.1.0",
  "description": "Atlas Squad Mission Control — Backend API",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create mission-control-db",
    "db:migrate": "wrangler d1 execute mission-control-db --remote --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute mission-control-db --local --file=schema.sql",
    "db:seed": "wrangler d1 execute mission-control-db --remote --file=seed.sql",
    "db:seed:local": "wrangler d1 execute mission-control-db --local --file=seed.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
