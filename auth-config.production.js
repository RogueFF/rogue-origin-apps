/**
 * PRODUCTION AUTHENTICATION CONFIG
 *
 * ⚠️ This file IS committed to Git and deployed to GitHub Pages
 * Password is VISIBLE in source code (client-side security only)
 *
 * For local development, use auth-config.js instead (not committed)
 */

const AUTH_CONFIG = {
  // Production password (visible in source - change this!)
  password: 'RogueWholesale2026',

  // How long to stay logged in (in days)
  sessionDuration: 30,

  // Pages that require authentication
  protectedPages: ['orders.html']
};
