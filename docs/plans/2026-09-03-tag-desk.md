# Tag & Desk v1 — Implementation Plan

**Goal:** Ship the redesigned Supply Kanban ("Tag & Desk") as a new page on GitHub Pages, reading and writing the live `/api/kanban` actions, so Koa can run it beside `kanban.html` and migrate when ready.

**Architecture:** One ESM page (`src/pages/tag-desk.html`), a pure model module the tests cover, thin API wrappers over `src/js/shared/api.js`, a `localStorage` desk-memory store for the lifecycle state that has no tables yet, and a render module ported from the reviewed prototype. Worker untouched. Design: `2026-09-03-tag-desk-design.md`.

**Tech stack:** vanilla ES modules on Pages, `node --test` unit tests (`tests/*.test.mjs`), Playwright smoke spec with mocked routes, `tools/stamp-modules.mjs` for the import map.

**Repo:** `C:\Users\Koasm\Desktop\Dev\rogue-origin-apps`, worktree `..\rogue-origin-apps-tag-desk`, branch `feat/tag-desk` off `master@7ad15ef3`. Front-end ships by pushing `master`.

**Conventions:** head copied from `floor.html`/`wholesale.html` (pre-paint theme script, PWA metas, `shared-base.css` first); no CDN icon fonts; one `<script type="module">` entry; `npm run stamp` writes the import map; every commit ends with `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

---

### Task 1: pure model — `src/js/tag-desk/model.js` + `tests/tag-desk-model.test.mjs`
Port `normalize.py` + `model.py` from the review: `zoneOf`, `num`, `deliveryDays`, `parsePrice`, `normalizeCard`, `buildModel({cards, cart, orders, requests, today, levelsChanged})`, check-day dates (`nextCheckAfter`, D1..D3), `cardState`. Apply Koa's data answers at normalize time (glove pack size 100; Grove Rack → Grove bags rack; T-Zero rack vs room). Tests pin: check days from a Thursday, lead-time-aware due, received → never due again same day, re-scan idempotence rule, note → RED/URGENT signals, the strapping merge.

### Task 2: `api.js`, `store.js`, `labels.js`
`api.js`: `getCards, getCart, getOrders, getRequests, addToCart, setCartQty, removeFromCart, markOrdered, updateCard, addCard, deleteCard`. `store.js`: versioned desk memory with defaults. `labels.js`: the EN/ES table with `%var` substitution; language from `shared/i18n.js`.

### Task 3: page + CSS + render + main
`tag-desk.html` (head, sprite, rail links, five views, dialogs, print area, module entry), `tag-desk.css` (prototype CSS on shared-base tokens, light + dark, phone, `@media print`), `render.js` (Order / Cards / Print / Grove / Tag), `main.js` (boot, `?flag=` Tag route, reload after every write, language and theme).

### Task 4: register + verify
Rail entry in `src/pages/index.html` (Office), `sw.js` STATIC_ASSETS + `CACHE_VERSION`, `npm run stamp`, `npm run lint`, `npm test`, `npm run stamp:check`, Playwright smoke against mocked routes at 1440 and 390.

### Task 5: ship
Commit on `feat/tag-desk`, merge to `master`, push (Pages deploys), verify the live URL with the real API (read-only checks first, then one real scan/undo on a throwaway card), record in the wiki.
